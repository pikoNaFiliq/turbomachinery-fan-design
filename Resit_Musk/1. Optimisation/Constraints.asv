function [c,ceq] = Constraints(x)

global couplings
    x0 = couplings.x0;
    
    x = x .* abs(x0)
    
    %% Set the design variables
    [phi,psi,power] =  deal(x(1),x(2),x(3));
    
    %% U blade c